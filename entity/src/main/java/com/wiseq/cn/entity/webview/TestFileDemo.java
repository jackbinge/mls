package com.wiseq.cn.entity.webview;



/**
 * @ClassName TestFileDemo
 * @Description TODO
 * @Author silver
 * @Date 2018/11/1 15:09
 * @Version 1.0
 **/

public class TestFileDemo {
  /*  private  String name;
    @HdfsFileName
    private static String hdfsName;

    public String getName() {
        return name;
    }

    public static String getHdfsName() {
        return hdfsName;
    }

    static {
        Field[] fields = TestFileDemo.class.getDeclaredFields();
        for (Field field : fields) {

            boolean isusedAnnotation = field.isAnnotationPresent(HdfsFileName.class);
            if (isusedAnnotation) {
                HdfsFileName annotation = field.getAnnotation(HdfsFileName.class);

                try {
                    field.setAccessible(true);
                    Date d = new Date();
                    SimpleDateFormat sdf = new SimpleDateFormat("yyyyMMddHHmmss");
                    field.set(annotation, sdf.format(d)+"_"+RadomThree.getThreeNumber());
                } catch (IllegalAccessException e) {

                }
            }
        }
    }


*/
}
